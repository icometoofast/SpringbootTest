<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.springboot.withHtml.mapper.loginAndSignAndForgetpwdMapper">

    
    <select id="login" resultType="INTEGER" parameterType="String">
        select count(*) from SpringBootTest.user where email = #{email} and password = #{password}
<!--        <where>-->
<!--            <if test="username != null and username != ''">-->
<!--                name = #{username}-->
<!--            </if>-->
<!--            <if test="password != null and password != ''">-->
<!--               and  password = #{password};-->
<!--            </if>-->

<!--        </where>-->
    </select>

    <select id="isRepeatSign" resultType="INTEGER" parameterType="String">
        select count(*) from SpringBootTest.user where email = #{email}
    </select>

   <insert id="insertEmailAndPwd">
       insert into SpringBootTest.user(email,password) values (#{email},#{password});
   </insert>



</mapper>